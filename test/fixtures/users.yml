# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  email: "user1@example.com"
  password_digest: <%= BCrypt::Password.create('password123') %>
  # Encrypted fields will be set by model callbacks
  # giae_username, giae_password, giae_school_code are encrypted
  # We set them after_create in tests that need them
  email_notifications_enabled: true
  in_app_notifications_enabled: true

two:
  email: "user2@example.com"
  password_digest: <%= BCrypt::Password.create('password456') %>
  email_notifications_enabled: false
  in_app_notifications_enabled: true